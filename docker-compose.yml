services:
  postgres:
    # pgvector/pgvector:pg16, not stock postgres:16-alpine: Phase 3d's
    # schema-embeddings table needs the pgvector extension (CREATE EXTENSION
    # vector, see alembic/versions/ -- the migration adding schema_embeddings).
    # Same single sqlstudio database/cluster serves both the control plane
    # and (via TEST_POSTGRES_URL in CI) the connector test suite, so one
    # image swap covers both.
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: sqlstudio
      POSTGRES_PASSWORD: sqlstudio
      POSTGRES_DB: sqlstudio
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      # Host-side 5434 to avoid colliding with any other local Postgres —
      # containers still talk to each other over the compose network at the
      # standard internal 5432, see CONTROL_PLANE_DB_URL below.
      - "5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sqlstudio"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      # Host-side 6381 to avoid colliding with any other local Redis —
      # same reasoning as postgres's 5434 mapping above.
      - "6381:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama

  # One-shot: runs the real Alembic migration chain to completion, then
  # exits. app/worker/scheduler all gate on this via
  # `condition: service_completed_successfully` instead of racing each
  # other against Postgres directly -- found via an actual clean-machine
  # `docker compose down -v && docker compose up` run (Phase 6's own
  # done-when bar): with app running `alembic upgrade head` in its own
  # entrypoint and worker/scheduler only waiting on Postgres being
  # healthy (not on app's migrations finishing), the scheduler's very
  # first tick queried `scheduled_queries` before that table existed --
  # "relation \"scheduled_queries\" does not exist" -- a real crash on a
  # genuinely fresh volume that every previous phase's CI never caught,
  # because CI boots the API process directly (`uvicorn app.main:app`),
  # never `docker compose up` from a clean volume.
  migrate:
    build: .
    environment:
      CONTROL_PLANE_DB_URL: postgresql+asyncpg://sqlstudio:sqlstudio@postgres:5432/sqlstudio
    depends_on:
      postgres:
        condition: service_healthy
    command: alembic upgrade head
    working_dir: /app/backend

  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      AI_MODE: ollama
      OLLAMA_BASE_URL: http://ollama:11434
      # mistral:7b, not qwen2.5-coder:7b -- empirically verified (task #22)
      # to be the more reliable single default: qwen3's "thinking" mode
      # swap-thrashed a 16GB dev machine (89-100s+ for the simplest query,
      # then stalled entirely), and llama3:latest -- faster -- produced
      # genuinely invalid SQL on a complex multi-join question. mistral was
      # slower than llama3 but never produced invalid SQL in that check.
      OLLAMA_MODEL: mistral:7b
      CONTROL_PLANE_DB_URL: postgresql+asyncpg://sqlstudio:sqlstudio@postgres:5432/sqlstudio
      REDIS_URL: redis://redis:6379/0
      JWT_SECRET: insecure-dev-secret-change-me
    volumes:
      - ./backend/data:/app/backend/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      ollama:
        condition: service_started
      migrate:
        condition: service_completed_successfully
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    working_dir: /app/backend

  worker:
    build: .
    environment:
      AI_MODE: ollama
      OLLAMA_BASE_URL: http://ollama:11434
      OLLAMA_MODEL: mistral:7b
      CONTROL_PLANE_DB_URL: postgresql+asyncpg://sqlstudio:sqlstudio@postgres:5432/sqlstudio
      REDIS_URL: redis://redis:6379/0
      JWT_SECRET: insecure-dev-secret-change-me
    volumes:
      - ./backend/data:/app/backend/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      ollama:
        condition: service_started
      migrate:
        condition: service_completed_successfully
    command: python -m app.worker
    working_dir: /app/backend

  scheduler:
    # Phase 4b: enqueues due scheduled queries onto the worker's
    # scheduled_queries queue every 30s. Deliberately its own process, not
    # a thread inside the worker service above -- see app/scheduler.py's
    # own docstring for why (fork-safety: a persistent thread doing DB
    # queries + Redis enqueues could hold library locks across the
    # worker's fork() for an unrelated job).
    build: .
    environment:
      CONTROL_PLANE_DB_URL: postgresql+asyncpg://sqlstudio:sqlstudio@postgres:5432/sqlstudio
      REDIS_URL: redis://redis:6379/0
      JWT_SECRET: insecure-dev-secret-change-me
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    command: python -m app.scheduler
    working_dir: /app/backend

volumes:
  pgdata:
  ollama_models:
