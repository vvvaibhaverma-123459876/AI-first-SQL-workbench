# Python
__pycache__/
*.pyc
*.pyo
backend/.venv/
backend/.env
backend/data/*.db

# Node
frontend/node_modules/
# package-lock.json IS tracked (see below) — CI's `npm ci` needs it for
# reproducible installs.

# Build output (regenerated by start.sh)
frontend/dist/
*.tsbuildinfo

# OS
.DS_Store
