fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
pandas==2.2.3
sqlglot==25.20.2
python-dotenv==1.0.1
requests==2.32.3
pytest==8.3.3
httpx==0.27.2
# Optional for AI_PROVIDER=hf. Ollama mode does not need it.
# transformers==4.45.2

# v2 control-plane: auth, workspaces, multi-user Postgres backend.
fastapi-users[sqlalchemy]==15.0.5
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.13.3
pytest-asyncio==0.24.0

# v2 data connectors: Postgres and MySQL are live-tested in this project's
# own test suite (test_connections.py) against real Docker containers.
cryptography==49.0.0
psycopg2-binary==2.9.12
pymysql==1.2.0
# SQLite needs no extra driver (stdlib). Snowflake, BigQuery, and Databricks
# are implemented against their standard SQLAlchemy dialects in
# app/connections/drivers.py but are NOT installed by default: none can be
# live-tested without a real cloud account (none exists in this project's
# dev/CI environment), and each pulls a large, independent dependency tree
# (boto3, grpcio, pyarrow) that would slow every install and widen the
# version-conflict surface for a codebase pinned this tightly, for a code
# path nobody is running by default. Uncomment the one(s) you actually use --
# no code changes needed, drivers.py imports them lazily on first use.
# snowflake-sqlalchemy==1.11.0
# sqlalchemy-bigquery==1.17.0
# databricks-sqlalchemy==2.0.10

# v2 Phase 3: RQ worker so AI calls (10-100s+ each, measured empirically)
# run off the request path instead of blocking a FastAPI route.
rq==2.10.0
redis==8.0.1

# v2 Phase 3d: pgvector-backed per-connection schema embeddings for
# semantic table retrieval (replaces keyword matching in suggest_tables()
# when a real connection is attached). Needs the pgvector Postgres
# extension too -- see docker-compose.yml's postgres image.
pgvector==0.5.0

# v2 Phase 4b: scheduled queries. Pure cron-expression math only -- the
# actual periodic enqueue loop is app/scheduler.py, a standalone process
# (see its own docstring for why it's not a thread inside app/worker.py).
croniter==6.2.4
